//! The application core: tools, selection, viewport, command bus, preferences.
//!
//! UI-toolkit agnostic on purpose, so that the same core drives both the
//! desktop application and the headless command line tools.
