---
type: comment
item: XARA-T-0001
author: mcp
created: 2026-09-23T10:00:36Z
kind: comment
---

Done in commit 2c3564c (branch worktree-agent-a193c011b72a4843c).

`xarast_shell::intents::IntentAdapter` + `semantic_modifiers`/`semantic_button`: a thin adapter module in xarast-shell (not in `input::translate`, names no winit type; xarast-app cannot see `ShellEvent`). Maps Ctrl/Shift/Alt → constrain/adjust/alternative (+ snap toggle), tip/barrel → primary/secondary, middle-drag → Pan, wheel → Pan, constrain+wheel → Zoom about the pointer (√2 per notch), pinch/pan gestures, canvas-relative samples, DPI = 96 × scale. 18 tests over scripted sequences, one driving a real Session. Also 5a87d29 fixes clippy 1.98 lints workspace-wide so the gate passes on the current stable.
