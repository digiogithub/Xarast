---
type: comment
item: XARA-T-0002
author: mcp
created: 2026-09-23T10:05:50Z
kind: comment
---

Done in commit 5371ab2 (branch worktree-agent-a193c011b72a4843c).

`xarast_app::render_thread`: `RenderRequest::{Frame(FrameJob), Cancel{up_to_generation}, Shutdown}`, generations allocated by `RenderThread::submit` (main-thread counter), one channel type = a latest-wins mailbox with one slot each way (waiting frames superseded, uncollected results replaced, submit never blocks), cancellation drops waiting and in-flight frames. `FrameJob` carries `Arc<DisplayList>` + `Arc<Resolver>` snapshot + `ViewParams` (deviation from the §U5.5 sketch: the resolver is added, per app-core invariant 6). `Session::frame_job` builds it. 8 tests, including deterministic in-flight supersession via a gated `FrameRenderer`. No crossbeam dependency: std `Mutex`+`Condvar` is the whole mailbox.
