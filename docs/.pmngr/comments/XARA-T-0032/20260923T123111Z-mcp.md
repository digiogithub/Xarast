---
type: comment
item: XARA-T-0032
author: mcp
created: 2026-09-23T12:31:11Z
kind: comment
---

Done in `d6552d8`. Vertical ruler labels are now rotated 90° anticlockwise with `TextShape::with_angle` and read up the strip from just above their tick, at 9 pt (the horizontal ruler's size). The unit suffix is kept.

- **Test.** `rulers::tests::vertical_labels_fit_the_strip_at_every_scale` lays out every label the ruler produces, across all units and zooms, at scales 1, 1.25 and 2. It asserts that each label fits across the 18 pt strip and ends before the next one.
- **Window.** Verified with `xarast --screenshot` on BLUECAR.xar at 1×: `1200pt`…`600pt` are fully legible.
