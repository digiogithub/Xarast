---
type: comment
item: XARA-T-0060
author: mcp
created: 2026-09-23T14:14:06Z
kind: comment
---

Done in d77ff7f (`xarast_app::recent::RecentFiles`, `AppState::with_recent_store`) + cdbb83f (the binary uses `default_store_path()`). `$XDG_STATE_HOME/xarast/recent`, fallback `~/.local/state/xarast/recent`; header `xarast-recent 1`, one absolute path per line, max 10, atomic write, pruned at load, failed opens forgotten, corrupt input → empty list. Tests: 7 unit in `recent.rs`, `tests/open.rs::the_recent_list_persists_prunes_and_forgets_what_fails`.
