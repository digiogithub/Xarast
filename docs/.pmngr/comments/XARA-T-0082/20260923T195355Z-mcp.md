---
type: comment
item: XARA-T-0082
author: mcp
created: 2026-09-23T19:53:55Z
kind: comment
---

Done in `xarast-app::thumbnail` (`CpuThumbnails`, `thumbnail_png`) — commits b099fe4, f196095 (`SaveOptions::thumbnail`), 2895c19 (`prepare_save`/`prepare_resave`), 8f7487f (render beside the SVG serialisation, parallel CPU config).

- Active spread's first page edge to edge, 256 px longer side, page colour beneath; passes `thumbnail::check_png` (unit test + every corpus file in `tests/save.rs`; 50/59 show ink).
- Every File › Save writes it; a re-save never carries the source's.
- ProbeX16 (518 k nodes): thumbnail alone ~0.4 s, but rendered on a scoped thread next to the SVG serialisation it adds ~40 ms of wall time (save 1.09 s with vs 1.05 s without, tmpfs).
- Previews (F5.9) are not written yet; `CpuThumbnails::preview` renders spread 1 only.
