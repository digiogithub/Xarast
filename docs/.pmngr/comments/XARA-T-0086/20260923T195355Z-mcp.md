---
type: comment
item: XARA-T-0086
author: mcp
created: 2026-09-23T19:53:55Z
kind: comment
---

Done — commits b099fe4 (`xarast-app::locks`, lock UX in `AppState`), b9e7d48 + 9831b7a (`xarast-shell::signals`).

- Opening a `.xarast` takes its lock (`steal_if_stale`: a dead holder on this boot is reclaimed silently). Held → "Document in use" prompt: Open read-only (Save asks for a new name) / Open a copy (untitled "name (copy)") / Force (risky) / Cancel. Unavailable → opens unlocked with an Info diagnostic. Saving to a new target takes its lock; a target another session holds is refused.
- Released on close (session drop) and on quit. `HeldLock` registers every lock so `locks::release_all()` can remove this process's files.
- SIGINT/SIGTERM/SIGHUP via `signal-hook`'s safe iterator (MIT/Apache, no `unsafe` of ours; cargo deny clean): autosave modified docs, drop every session, exit `128+sig`; fallback after 5 s or a second signal removes the lock files itself.
- Evidence: `tests/save.rs::two_sessions_contend_for_one_file`, `a_stale_lock_from_a_dead_process_is_taken_without_asking`; live on COSMIC: a real window holding a `.xarast` lock, `kill -TERM` → orderly shutdown in 214 ms, lock file gone, exit 143.
- Not done: surfacing `XarastReader::suggests_read_only()` as its own UX (container diagnostics still go to the problem list).
