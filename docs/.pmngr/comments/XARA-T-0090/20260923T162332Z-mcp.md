---
type: comment
item: XARA-T-0090
author: mcp
created: 2026-09-23T16:23:32Z
kind: comment
---

Done on `claude/xara-xtreme-rust-port-ktggmu` worktree branch: 9809e28 (workspace flate2 = zlib-rs, seeds regenerated, pins), 4e932f8 (runtime_detection), docs 227b10a.

- Decision: **zlib-rs** for the whole workspace, chosen on the workspace `flate2` line (`default-features = false, features = ["zlib-rs", "runtime_detection"]`), Zlib licence, cargo deny green. Only pure-Rust candidates are miniz_oxide and zlib-rs (zopfli far too slow).
- Numbers (pinned 4-7): container `save_20mb` 273 → 89 ms, `save_20mb_atomic` 285 → 98 ms, `resave_300mb_raw` 104 → 91 ms, open unchanged. ProbeX16 package write 966 → 337 ms; packages ~2 % larger.
- `.xar` import at parity only with `runtime_detection` (without it: ProbeX16 import 356 ms > 350 budget in xarast-xar's own build): miniz 293/416 ms vs zlib-rs 288/419 ms (ProbeX16 / corpus).
- Determinism: `deflate_backend_is_pinned` (raw DEFLATE, levels 6 and 1) + re-pinned `deterministic_bytes_are_pinned`; both unchanged between scalar and SIMD builds → CPU-independent output. `.xar` fuzz seeds (bad-crc, bad-length, two-blocks-streamed-record ×4 targets) regenerated; import snapshot test unchanged over the corpus.
