---
type: comment
item: XARA-T-0101
author: mcp
created: 2026-09-23T16:23:32Z
kind: comment
---

Done: d8897af (passes 4–5, `svg/style.rs`), e7e0eb4 (path-data separator bug fix + stack number formatting), docs 227b10a. Reader follow-up filed as XARA-T-0107 under XARA-US-0024.

- Pass 4 hoists slot properties (fill/stroke family + `xarast:fill-ref`/`stroke-ref`, profile-inherited) onto any `<g>` when every painting child agrees; text, images, foreign fragments, nested `<svg>` block. Pass 5: ≥ 8 slots sharing ≥ 3 properties → `class="cN"`, one `<style>` first in `<defs>`, twins in `<xarast:paint-class>` (resvg breaks on CSS custom properties). `SvgOptions { hoist, classes }` test each alone.
- Size: ProbeX16 SVG 56.7 → 50.8 MB (−10.4 %), corpus 103.1 → 96.5 MB (−6.4 %).
- Rendering: resvg old vs new **pixel-identical on 59/59** (mean SSIM vs CPU reference 0.9416 → 0.9416); Inkscape identical on 8 files; Chrome identical on 7, one file ≤ 11/255 on 0.09 % px (SSIM 0.99999). The path fix then changes 11 files, all closer to the reference (mean 0.9416 → 0.9423; Groucho2 0.962 → 0.991).
- ProbeX16 save: **1.60 s → 0.79 s** (SVG 453 ms + package 337 ms, medians of 7) vs 1 s budget. Corpus 2.67 s → 1.59 s. `svg/write_100k` 34.2 → 30.6 ms, `svg/save_100k` 114.9 → 63.0 ms.
