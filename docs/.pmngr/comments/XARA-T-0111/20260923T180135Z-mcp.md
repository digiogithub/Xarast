---
type: comment
item: XARA-T-0111
author: mcp
created: 2026-09-23T18:01:35Z
kind: comment
---

Done in 2edbc2b. The stroke's own transparency now has its own elements: `<xarast:stroke-transparency>`, `xarast:stroke-mask="url(#m…)"` (the mask is referenced now) and `xarast:stroke-blend`. When `xarast:stroke-blend` is present, `xarast:blend` means the fill's mode only. The bitmap transparency twin carries href/xlink:href, which counts as one reference. Transparency twins are now complete: levels, profile, procedural parameters and mapping. The reader still accepts the old spellings. Tests: `what_svg_cannot_draw_reads_back_exactly` and `a_lone_stroke_transparency_twin_is_the_strokes`.
