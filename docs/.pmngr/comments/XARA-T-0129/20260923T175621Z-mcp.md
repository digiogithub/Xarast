---
type: comment
item: XARA-T-0129
author: mcp
created: 2026-09-23T17:56:21Z
kind: comment
---

Done on branch `worktree-agent-a0d4f492948290eea`: d94dce8 (xarast-image: tag-68 PNG alpha is transparency), fc36d0e (xar: normalise tag-68 PNG alpha + keep JPEG8BPP palette, T10.2.6), 3ba98d6 (bitmap fills tile by mapping, XARA-T-0054), eb375af (walker decodes; images_failed; negative cache; parallel decode), 6471b79 (docs).

Evidence:
- `images_pending == 0` and `images_failed == 0` on all 59 corpus files (new test `every_corpus_bitmap_decodes_and_is_drawn`; 23 files carry bitmaps). Before: 39 objects over 22 files (smoke-open over the 73 `.xar` in the fork).
- Headless renders checked visually: leafgirl, Spitfire, WATCH, Groucho2, feathers, scope3 simple, Fill Types simple (bitmap row: single / repeating / mirrored / contone / duotone), TestBitmapFill, TextDesigns.
- Found and fixed: a tag-68 PNG with an alpha channel stores transparency (0 = opaque; `research/01 §4.5`). All 22 alpha PNGs in the corpus rendered as black boxes until the importer normalised them.
- Real window (`xarast --screenshot`, GPU tiles): leafgirl and scope3 render; cold start unchanged within noise (≈ 330–370 ms).
- Stripped `xarast` +523 KiB (+2.5 %); no libwayland/GL/Vulkan in ldd.
- `.xar → .xarast → reload` keeps the bitmaps; four writer gaps now visible and listed in `KNOWN_RENDER_GAPS`: XARA-T-0154 (palette), XARA-T-0155 (duotone), XARA-T-0109 (bitmap mapping), XARA-T-0111 (bitmap transparency image).
- Gates: fmt, clippy -D warnings, 1190 tests with the corpus, cargo deny all pass.
