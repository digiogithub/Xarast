---
type: comment
item: XARA-T-0152
author: mcp
created: 2026-09-23T18:10:21Z
kind: comment
---

Done on branch worktree-agent-aed9fb969e4328a5c: 16b342f (core: pick by painted fill and stroke through the geometry hit index), b0ac6ed (pick bench). `xarast-app/src/picking.rs`: `Picker` builds a `HitIndex<NodeId>` over ink leaves of visible/unlocked/non-guide layers (z = render rank << 16, bounds with stroke reach), tests each candidate with `HitShape` from resolved attributes (fill only if painted, stroke only if line colour not none), maps to top group; `PickMode::{TopGroup, Leaf (Ctrl), Under (Alt)}`; marquee via `query_rect(Touch)` → top groups whose bounds are enclosed. Objects with no testable leaf (text, unrendered live) fall back to bounds. Built lazily on first pick after a change (session invalidates in `after_mutation`). Bench at 100k objects: warm pick 2.3 µs, rebuild 62 ms. Tests: `tests/picking.rs` (4). Follow-up: incremental index updates per committed transaction.
