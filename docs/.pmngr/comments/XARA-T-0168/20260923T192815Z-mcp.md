---
type: comment
item: XARA-T-0168
author: mcp
created: 2026-09-23T19:28:15Z
kind: comment
---

Done on branch worktree-agent-a67f966fcb3c4b9c3: 389549c (xarast-doc change journal: `Tree::drain_changes`, `ChangeLog`/`TreeChange`, appended by every `Action::apply`), c6519e5 (picker applies the journal lazily: removes and re-walks only the touched top-level objects, z per top in 2^24 gaps; rebuilds on overflow/layer/spread/loose-attr changes). `bench pick` "undo + first pick after it": 2.26 ms per undo+pick+redo+pick ≈ 1.1 ms per edit+pick (was a 62 ms rebuild); undo itself untouched (journal drained, not processed, on the undo path). Tests: incremental index == fresh index after moves, delete, undo/redo, group/ungroup, paste, z-order (tests/picking.rs, tests/structure.rs); rebuild counter proves no rebuild. Image-alpha picking not done (left in tools.md TODOs).
