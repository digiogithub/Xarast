---
type: comment
item: XARA-T-0218
author: mcp
created: 2026-09-24T08:50:37Z
kind: comment
---

## Report — font embedding done (branch `worktree-agent-ab43a0cf8dd7e9d59`, NOT merged with the working branch yet)

Commits: `db46282` (core: embedding layer), `01e95d4` (io: WOFF2 in .xarast/SVG), `0416a42` (io: PDF), `efe6087` (io: `--text`, outline fallback), `6dc4b52` (docs).

**What is done (item 1 of the description, criterion 13 of US-0050):**
- One shared layer, `xarast_text::embed`: the `OS/2.fsType` rule (restricted and bitmap-only refused, no-subsetting → whole glyph set, preview&print allowed because Xarast only draws the subset), `subsetter` 0.2.6 (MIT OR Apache-2.0, no default features), our own `cmap` builder and our own null-transform WOFF2 writer over `brotli` 9 (BSD-3-Clause AND MIT). `cargo deny` passes, no exception needed.
- `.xarast`: one `resources/fonts/b3-*.woff2` per face drawn with, `@font-face` rules in `<defs>`, each run's family chain lists the faces its characters use (fallbacks included), `xarast:font-embed="denied"` on runs of a refused face (no file for it), `xarast:fonts` in `meta.xml`. The reader skips `@font-face`; re-saves are byte-identical (fresh and raw-copy), corpus round trip 59/59 still exact.
- Browser check: headless Chrome renders the embedded WOFF2 (probe family nobody has installed: hebrew.xar mean |Δ| 0.85 vs our PNG). `cargo xtask svg-render` / `export-check` now load an SVG's embedded fonts into resvg as a browser would: hebrew.svg 32.5 → 0.45 with the pinned fonts.
- Synthetic evidence (the corpus `embeddedFonts.xar` has no font data): `crates/xarast-app/tests/font_embedding.rs` (unzip: exactly one woff2, not the refused face; one `denied` mark).

**Not done → filed:** item 3 (gradient/bitmap fills in the story frame), the generic family without PANOSE and the `xarast:font-id` twin → **XARA-T-0275**. Reading embedded subsets back into Xarast (needs a per-document font overlay) and the profile C outline copy → **XARA-T-0276**. Item 2 (`<textPath>`) was closed by XARA-T-0252.
