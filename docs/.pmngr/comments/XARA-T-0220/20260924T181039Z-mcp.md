---
type: comment
item: XARA-T-0220
author: mcp
created: 2026-09-24T18:10:39Z
kind: comment
---

Done on branch `worktree-agent-affdbd3ed17114b79` (on top of 74aff7a), not pushed or merged:
- 33edb34 core: axis lock (Constrain on a centre or origin keeps it on a 45° axis) and aspect lock (Adjust on an elliptical radial or diamond axis turns the other axis with it). Commits two MoveFillControl per object: one step, and the render matches the preview.
- 3a25548 core: `GestureEvent::DragStart::count`. The second press of a double click, dragged, makes a conical fill. Adjust's circle still wins.
- 2965eca core: outline handle sets (`paint_sets`, `FillSet/FillSelection::slot`). `Intent::Nudge` with the six research/04 §4.5 steps; a run of nudges is one step. `Tool::status` plus a hover target give the status line and cursors (new `CursorKind::Pointer`). `Intent::InfobarDrag` previews a profile, stop position or level slider and commits one step; Esc, undo, redo and a tool switch drop it.
- 0695150 ui: the shell sends nudges for the arrows when a handle is selected (and pans otherwise), shows the tool status, and maps the slider drags. The toolbar slider sends Preview/Commit/Cancel.
- 72246f2, 7d560a1 docs: tools.md decisions 76–81 and invariant 17; ui.md; colour.md.

Hue blend mode: already done by US-0018/T-0256 (`TRANSP_MODES` has Hue). No work was needed.

Tests: fill_tool.rs +7, tool.rs +1, xarast-ui toolbar.rs +2 (kittest drag and Esc), shell +1. Each gesture is one step with exact undo, Esc restores digest and pixels, and damage stays on the edited object (slider test).

Gates, with the corpus, all exit 0: fmt, clippy, test (2056 passed, 0 failed, 1 ignored), doc.

Gaps filed as XARA-T-0308: colour drops on outline stops, outline drag-out, the original's minor-axis follow without Adjust, and the document nudge size (the 1 mm unit is provisional).
