---
type: comment
item: XARA-T-0222
author: mcp
created: 2026-09-24T13:03:15Z
kind: comment
---

Done on branch `worktree-agent-a9601a51e30be16e9`. Nothing is pushed or merged.

**Commits**
- b710f93 render: lock the fill shape x blend family golden matrix
- 0b743aa docs: record the fill x blend golden matrix in the render note

**What was built**
- `corpus::fill_blend_cases()` generates **160 scenes**: 8 fill shapes × 10 exposed families × {flat, graduated} transparency. This is the phase-08 acceptance item 6 minimum.
  - Shapes: flat, linear, circular, elliptical, conical, diamond, mesh3, mesh4.
  - Families (`EXPOSED_FAMILIES`): Mix, Stained Glass, Bleach, Contrast, Saturation, Darken, Lighten, Brightness, Luminosity and Hue. Bevel and None are renderer-internal, so they are left out.
- `tests/golden_fill_blend.rs` checks them with the existing golden tooling (gate A: exact, CPU, deterministic config). The goldens are in `tests/golden/fill_blend/`, at 48×48 and about 1.1 KiB each (183 KB in total). Regenerate them with `XARAST_UPDATE_GOLDEN=1`.
- A second test covers acceptance 14 for each cell: for every shape and variant, all 45 family pairs render different images.
- The matrix is kept out of `all_cases`, so the export, PDF, determinism and parity tests don't pay for 160 extra scenes.

**Note on XARA-T-0256**
- The graduated transparency uses the fill's own shape and frame for the five scalar shapes.
- For flat, mesh3 and mesh4 it uses a linear frame instead. A mesh-shaped `TranspSource::Gradient` evaluates to level 0 everywhere, and a golden would have locked that in.
- Every cell uses `Repeat::Simple`, so the missing mesh repeat support is neither exposed nor hidden here. Both mesh defects stay with T-0256.

**Gates**
- fmt: pass
- clippy `-D warnings`: pass
- workspace tests with the corpus: 1938 passed, 0 failed, 1 ignored
- cargo deny: pass
- build `--locked`: pass
