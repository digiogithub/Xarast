---
type: comment
item: XARA-T-0223
author: mcp
created: 2026-09-23T22:14:26Z
kind: comment
---

## Report (worktree branch `worktree-agent-a4799c1206ed73003`, on top of 8068f92)

Commits: 7777bb8 core: text insert/delete commands + grapheme boundaries; 7e209ca core: typing in the text tool, one undo step per burst; 936097a ui: route typing keys to the caret; cb637c1 docs: memory.

**What was built**
- `xarast-doc/src/text_edit.rs`: `InsertText`/`DeleteRange` commands (byte offsets into `StoryText::text`), `insert_text`, `delete_range`, `new_story`. New text takes the previous character's style; `\n` splits the line and copies the attributes in scope, so no character restyles; deleting a break lets the paragraph run on (no line merge). New `EditError::TextEdit`.
- `xarast-text`: `prev_grapheme`/`next_grapheme` (ICU4X extended grapheme clusters).
- `xarast-app`: `EditCommand::{TypeText, DeleteText, CreateText}` with `CoalesceKey { gesture: burst, kind: "text-typing" | "text-delete" }`; `Intent::TextInput(TextInput { kind, time_ms })`; `Tool::text_input` and `Tool::after_commands(doc, created)` (called by `run_tool` after a tool's commands applied). The text tool keeps a burst: same kind, same story, caret where the last key left it, < 500 ms, and the document epoch unchanged since its own edit (so undo or any other command ends it). The first character at a pending caret creates the story (active layer, current attributes) in the same undo step.
- `xarast-shell`: while a caret is up, Backspace/Delete (Ctrl = word), Enter and any key with printable text or tab (no Ctrl/Alt) become `TextInput`, stamped with `IntentAdapter::now_ms`.

**Acceptance criteria**
- 200 characters = one undo step, one Ctrl+Z restores the digest: `typing_200_characters_is_one_undo_step`. Met.
- Backspace/Delete remove whole grapheme clusters, caret valid after reflow: `backspace_and_delete_remove_whole_grapheme_clusters`, `a_column_wraps_as_it_is_typed_and_the_caret_follows`. Met.
- Pending point story/column becomes a story on the first character; nothing left otherwise: `the_first_character_at_a_pending_caret_creates_the_story`. Met.

**Gates**: fmt 0; clippy -D warnings 0; `XARAST_XAR_CORPUS=… cargo test --workspace` 0 (1616 passed, 0 failed, 1 ignored); cargo deny 0; build --locked 0.

**Follow-up filed**: XARA-T-0237 (a story emptied by deletion stays; the original removes it). Also noted in text.md: attributes set while the caret is up are not applied to typed text yet (needs `SetTextAttr`, T9.2.4).
