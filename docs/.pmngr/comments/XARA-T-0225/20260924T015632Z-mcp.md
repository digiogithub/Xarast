---
type: comment
item: XARA-T-0225
author: mcp
created: 2026-09-24T01:56:32Z
kind: comment
---

## Report: done, ready for review

Branch `worktree-agent-ad028f7dae60eab81`, based on 5166226. Commits:
- d7b1dee core: add the OpenType feature text attribute. New Xarast-only slot `TxtFeatures` / `AttrValue::FontFeatures`, appended as slot 47. Bridged to `StyleRange::features` and written to `.xarast` as `xarast:features`.
- 7aff7a3 core: add the SetTextAttr text command. This is T9.2.4 `SetTextAttr`. A character attribute becomes each item's own child. A paragraph attribute goes on every line of each paragraph the range touches.
- fcfe44f core: satisfy clippy in the feature round-trip test
- 11bdb8b core: drive text attributes from the text tool's infobar. The bar has font chooser, size, B/I/U, alignment, line spacing, tracking, the OpenType panel and the tab kind. It also carries the ruler data. Edits go to the selection as one `EditCommand::SetTextAttr` step. At a caret they become a pending style, applied to the next typed text in the same "Typing" undo step. A pending caret passes its style to the new story. With no caret they go to the selected stories; with nothing selected, to the current attributes (`ToolRequests::current`).
- 9af1e0d ui: draw the text ruler and drag its margins and tab stops. Each drag commits on release. Dragging a tab off the strip removes it, and a click adds one. Guides still work.
- b0f21b3 docs: text.md, tools.md (decisions 59–60, invariant 14), ui.md, document-model.md, xarast-format.md, research/06.

Gates:
- `cargo fmt --check`: pass.
- clippy with `-D warnings`: pass.
- Tests with the corpus: 1740 passed, 0 failed, 1 ignored.
- `cargo deny`: pass.
- `cargo build --locked`: pass.

Gaps filed: XARA-T-0263 (T9.3.8 centre/right/decimal tabs in layout) and XARA-T-0264 (script, baseline and aspect controls; spacing mode; line-start style; ruler edge cases).
