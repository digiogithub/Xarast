---
type: comment
item: XARA-T-0230
author: mcp
created: 2026-09-23T23:06:08Z
kind: comment
---

XARA-US-0060 covers the CI part of this task. The `check` job now installs poppler-utils, so the render comparison in `tests/pdf.rs` runs in CI. The new `export` job installs qpdf and runs `qpdf --check` on every fixture PDF through `cargo xtask export-check --require-tools`, and the nightly corpus workflow does the same for the corpus PDFs. qpdf is not installed locally, so the first CI run will be the first real `qpdf --check`. Still open here: multi-page output, XMP and the output intent. The output intent needs an sRGB profile of our own, see XARA-T-0249.
