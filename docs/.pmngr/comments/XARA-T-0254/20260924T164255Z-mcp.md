---
type: comment
item: XARA-T-0254
author: mcp
created: 2026-09-24T16:42:55Z
kind: comment
---

Report (branch worktree-agent-a17f848f02f0e6503, commits 9170fa6, 52cc138, 7a26588, docs 718f02f).

- **Command table:** `AppCommand::ShowPane(DockPane)` → `Intent::ShowDialog(Dialog::Pane)` → `PlatformRequest::ShowDialog`, the same route as the alignment panel. Keys are the original's gallery keys (research/04 §4.7): **F9** colour gallery, **F10** layers, **F11** bitmap gallery. The colour editor and Photo have no key. No pane needs a document.
- **Window menu** (between View and Help): lists every pane with its key, including the Photo tab.
- **`UiHost::show`:**
  - puts the panel back in the dock if the layout lacks it;
  - makes it and every container above it visible;
  - makes it the active tab of every tab group above it;
  - gives its tab the keyboard focus on the next frame.
- **Old layouts:** `load_layout` puts back every registered panel that a saved tree lacks. A layout from before Photo gets Photo as a tab next to the bitmap gallery. A panel with no docked neighbour becomes a new cell of the column.
- **Shell:** `Viewer::perform_requests` calls `Workspace::show_pane`.
- **Tests:** panel::tests +4; tests/menus.rs +2; viewer::tests +1 (Window › Photo, F11, F9, F10, and F9 with no document); command tests +1.
- **Gates (with the corpus):** fmt 0, clippy 0, test 0 (2025 passed, 0 failed, 1 ignored), doc 0.
- **Open:** the shell does not save the dock layout to preferences yet. The re-docking rule sits in the host so it applies once it does.
