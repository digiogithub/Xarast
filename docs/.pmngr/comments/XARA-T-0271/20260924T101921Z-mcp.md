---
type: comment
item: XARA-T-0271
author: mcp
created: 2026-09-24T10:19:21Z
kind: comment
---

Report (branch `worktree-agent-a557a1711e373accb`, based on `claude/xara-xtreme-rust-port-ktggmu` 0433a76; commits ca742a1, 1e1fe5f, 3272bc7, aacd29e).

- **T10.3.1**: `xarast-doc/src/bitmap_fill.rs` has `MoveBitmapControl` (with an aspect lock), `SetBitmapTiling` (sets the fill's own tiling and the mapping attribute together), and `SetBitmapDpi` (resizes about the centre; the "Natural Size" label when the image's own DPI is used). Pure helpers: `bitmap_virtual_points` / `bitmap_real_points`, `move_bitmap_control`, `set_bitmap_dpi`, `bitmap_fill_dpi`, `natural_length`. `move_control` delegates to them for bitmap fills.
- **T10.3.2**: `fill_handles` draws the original's virtual points: the centre moves the whole fill, and the middles of the two axis edges turn and stretch their axis. Facts from `Kernel/opgrad.cpp:3701-3747` and `Kernel/fillattr.cpp:14041-14215`. A fill in perspective shows four corners and the centre. Constrain turns an edge handle about the centre and Adjust locks the aspect. A drag previews through `Preview::attrs`, and the release commits one "Move Fill Handle" step. Infobar: Repeat inverted in the tiling choice, a Resolution (dpi) field and a Natural size button.
- The bottom-left orientation rule is untouched: only `paint::bitmap_frame` translates.
- Tests: `xarast-app/tests/bitmap_fill.rs` (6): the preview is pixel-identical to the commit, one step per edit, undo and redo exact, Esc leaves nothing, aspect lock, tiling, natural size and DPI. `xarast-shell/tests/bitmap_damage.rs` (2): every preview frame and the commit repaint only the object's bounds and match a full render byte for byte. Unit tests: 6.
- Gates: fmt 0; clippy `-D warnings` 0; `cargo test --workspace` with the corpus: 1863 passed, 0 failed, 1 ignored across 137 test binaries; `cargo deny` ok; `cargo build --locked` 0.
- Docs: `tools.md` decisions 64–67 and invariant 15, `ui.md`, `image.md`.
- Gaps filed: XARA-T-0284 (the original's perspective handle behaviour; stroke-slot handles).
