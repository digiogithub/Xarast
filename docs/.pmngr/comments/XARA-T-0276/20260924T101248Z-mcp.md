---
type: comment
item: XARA-T-0276
author: mcp
created: 2026-09-24T10:12:48Z
kind: comment
---

Report (branch of worktree agent-a7bd27acea5615fce, based on 0433a76; commits 494b940, 05ed604, 7e01a00, ea90521, c2ee206).

**Done**
- Reader: `@font-face` rules are parsed and their files (package resource or data: URI) kept as `EmbeddedFont` in `DocumentResources::fonts()`.
- Per-document overlay: `FontDb::overlay()` + `register_document_face()` (private family, never shadows, never listed, never a fallback); `fonts::for_document()` builds and caches overlays by file identity. Walker, text tool, picking, convert, headless, CLI export and the SVG text placer use it. Two documents never share faces.
- Priority rule: a document face is used only when the machine has no face of that family with the same weight/style/width (justified in text.md: the machine face is complete and keeps GSUB/GPOS; the embedded face beats a synthesis). Characters the subset lacks come from the machine's face of the family, then fallback. No `.notdef`.
- WOFF2: the encoder applies the glyf/loca transform (-14 % Latin, -7 % Arabic). The decoder reads the glyf/loca and hmtx transforms, checked against a `ttf2woff2` fixture. No new dependency.
- Tests: `xarast-app/tests/embedded_fonts_read.rs` has 3 tests. (1) With the bold face removed from the font dir, the document renders pixel-identical to the installed render. (2) Typing outside the subset uses real faces. (3) Two documents with the same family and different faces do not interfere. `embed.rs` +2, and the woff2 unit tests +5.
- Gates: fmt, clippy -D warnings, workspace tests with corpus (1847 passed, 0 failed, 1 ignored; round trip and TextDesigns goldens pass), cargo deny, build --locked all pass.

**Not done, filed**
- Profile C text-outline copy → XARA-T-0285.
- Web font polish (`@font-face` style), GSUB/GPOS in the subsets, the writer's substitute, and a browser probe of the transformed files → XARA-T-0286.
