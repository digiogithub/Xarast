---
type: comment
item: XARA-T-0307
author: mcp
created: 2026-09-24T17:00:44Z
kind: comment
---

Done on branch `worktree-agent-a47a177c2157e88a6`. Not pushed. Commits:
- 02d8a98 (render + walker);
- ce272f3 (SVG mask);
- b026560 (importer + census);
- 2d294db (pixel tests, with T-0306);
- 7ded465 (docs).

**Change.** The importer keeps `(start, end)` together with the mode in `TranspPaint::Bitmap::contone`.
- Direction comes from `Kernel/gradtbl.cpp:1421-1458` and `grndrgn.cpp:4225-4316`: grey 0 (black) maps to the start level and 255 (white) to the end, with the profile applied.
- `TranspSource::Image` gains `ramp: Option<RampId>`, an interned transparency ramp indexed by luma/255. The walker composites in `family_of(mode)`. A mode of `None` means opaque.
- The identity case (0..255, no profile) keeps `None`, so no other render or golden moved.
- The SVG browser mask now follows the levels. The default row is byte-identical.
- `inspect --fills` still names it `bitmap` and always prints `mode=`. The corpus has 3, all mix. A new corpus test in `fills.rs` pins this.

**Evidence against the embedded previews** (T-0248 method: 60 dpi page render, registered box-filtered crop, mean |Δ|):

| File | Before | After | Pixels that changed | Reversed-direction counterfactual |
|---|---|---|---|---|
| JagSS100 simple | 10.89 (9.42 at the new registration) | **5.27** | 401 px: 80.97 → **7.37** | 15.91 |
| scope3 simple | 18.41 | **18.38** | 156 px: 27.47 → **23.26** | 18.78 |

The corpus levels are 115/191/204 → 255, all Mix, on palettised PNGs with no alpha.

**Pixel tests** (`xar_clips_and_masks.rs`):
- a Stained Glass bitmap transparency over blue gives black, where Mix would give yellow;
- levels 115..255 give grey 115 on black texels and white on white texels.

**Gates:**
- The workspace gates are the same as T-0306 (all pass).
- Corpus export check: 59 files, 118 comparisons, 0 failures. JagSS100: svg 1.88, pdf 2.39. scope3: svg 1.26, pdf 2.36. `qpdf` is not installed on this machine, so `--require-tools` was dropped.
- `svg-check --interchange`: 59/59.
