---
type: comment
item: XARA-US-0010
author: mcp
created: 2026-09-23T10:32:18Z
kind: comment
---

**5/5 — `.xar` import, release, whole corpus** (new `cargo bench -p xarast-xar --bench import`, `XARAST_XAR_CORPUS=/www/Xarast/xara-xtreme`; median of 7 warm runs per file, file already in memory; two runs at load 6 and 19 agree to 0.1 %. A third run at load 34 was 1.5× slower and is discarded):

| What | Budget | Measured | Verdict |
|---|---|---|---|
| `ProbeX16.xar` (7.37 MB, 870k records, 518 346 nodes), full import | ≤ 350 ms (phase 3) | **644 ms** (parse only 129–141 ms) | **1.8× over** |
| Whole corpus, 59 files, 12.25 MB, 830 533 nodes | ≤ 3 s | **0.87–0.89 s** (parse 0.18–0.20 s) | passes |
| Per-file import | — | median 0.10 ms, p90 4.5–5.1 ms, max 644 ms | |
| Next largest: `20000GradFilledShapes50PCtransparent` (120k nodes) | — | 96–103 ms | |

Parsing is about 20 % of the import. The other 80 % is mapping into the document model, at roughly 1 µs per node. `ProbeX16` is the only corpus file above 5 MB. Its import alone already exceeds the 500 ms open-to-first-paint budget, so that budget is at risk before any rendering starts (task XARA-T-0009).

Deferred to later tasks (they need the wired viewer): XARA-T-0008 pan/zoom, XARA-T-0009 open-to-first-paint, XARA-T-0010 cold start.
