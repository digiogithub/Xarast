---
type: comment
item: XARA-US-0014
author: mcp
created: 2026-09-23T10:33:34Z
kind: comment
---

## Summary: ready for review

Branch `worktree-agent-affa1f2f0ca5f585e` (fast-forwarded onto d43efd2, then 14 commits: fb8f7b5..eecc310). Not pushed.

### Targets (11), 10 min each, 1 process each, shared 24-core machine
| Target | Execs | exec/s | Result |
|---|---|---|---|
| fuzz_xar_records | 299 k | 497 | clean |
| fuzz_xar_tree | 237 k | 394 | 1 finding, fixed, clean rerun |
| fuzz_xar_decode | 523 k | 870 | clean |
| fuzz_xar_import | 389 k | 646 | 3 findings, fixed, clean rerun |
| fuzz_xar_path | 70.2 M | 116 750 | clean |
| fuzz_xar_colour | 5.8 M | 9 706 | clean |
| fuzz_path_boolean (new) | 69 k | 115 | clean |
| fuzz_svg_path_parse (new) | 12.9 M | 21 481 | 1 finding, fixed, clean rerun |
| fuzz_display_list (new) | 565 k | 940 | 1 open render gap (XARA-T-0022), target bounded; clean |
| fuzz_ramp (new) | 4.66 M | 7 759 | 1 finding, fixed, clean rerun |
| fuzz_doc_builder (new) | 3.8 M | 6 330 | 2 findings, fixed, clean rerun |

The .xar targets were additionally seeded, locally only, with the 59 real files (never copied into the repo).

### Findings fixed (each with a regression test that fails before the fix)
- xar: unresolved TAG_NODE_BITMAP counted as mapped twice (fb8f7b5)
- xar: children of grid records / defaults never counted (9391a49)
- xar: `N DOWN a UP DOWN b UP` dropped `a` from the tree — real data loss (64a31e0)
- doc: dangling colour ref made `finish` return Inconsistent (286682e)
- doc: sourceless live controller at the depth limit, attach error ignored (1cc7984)
- render: NaN ramp offsets made std sort panic (86698cf)
- geom: SVG arc radius 8e77 -> 1.8 GB kurbo allocation (14feba2)

### Open
- XARA-T-0022 (todo): CPU backend strokes/dashes whole paths before viewport clipping; memory exhaustion at deep zoom. fuzz_display_list bounds geometry and dash count until fixed.

### CI
`.github/workflows/fuzz.yml` (a1ebeec): nightly 02:30 UTC matrix over all 11 targets, 300 s each (configurable via workflow_dispatch), fails on crash/OOM/timeout and uploads the reproducer; grown corpus kept in the Actions cache.

### Gates
fmt, `cargo test --workspace`, `cargo deny check licenses`: pass. Per-crate clippy fails only on pre-existing rustc 1.98.1 lints (XARA-T-0015 class) — xarast-xar (cur.rs, decode.rs, import.rs:752, report.rs, tags.rs), xarast-render (golden.rs, cpu.rs, surface.rs), xarast-doc (history.rs:335); none in lines this story touched.
