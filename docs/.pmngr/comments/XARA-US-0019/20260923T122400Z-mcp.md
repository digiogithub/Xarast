---
type: comment
item: XARA-US-0019
author: mcp
created: 2026-09-23T12:24:00Z
kind: comment
---

Distro smoke matrix run 2026-09-23 (podman, bare images, no display, no FUSE, `packaging/linux/smoke-test.sh`):

| Distro | glibc | version/help/selftest | selftest-window | loader | xarast-cli render |
|---|---|---|---|---|---|
| Debian 12 | 2.36 | pass | skipped, exit 0 | 3 libs, none missing | pass |
| Fedora 44 | 2.43 | pass | skipped, exit 0 | pass | pass |
| openSUSE Tumbleweed | 2.44 | pass | skipped, exit 0 | pass | pass |
| openSUSE Leap 15.6 | 2.38 | pass | skipped, exit 0 | pass | pass |
| Arch (SteamOS-like) | 2.44 | pass | skipped, exit 0 | pass | pass |
| Ubuntu 20.04 (negative control) | 2.31 | fails as expected (GLIBC_2.32+ missing) | | | |

No libwayland/xkbcommon/X11/xcb/vulkan/GL/EGL in the loader list (all dlopen'd). Host Wayland session (NVIDIA RTX 4000 Ada, Vulkan): `--selftest-window` ok (cold start 395 ms), `--screenshot` of `Designs/amurdove.xar` wrote 974x1366 PNG and exited.

CI (`appimage.yml`): new `metadata` job on ubuntu-24.04 runs `desktop-file-validate` + `appstreamcli validate --no-net --strict` (moved out of the 22.04 container, whose desktop-file-validate 0.26 rejects `Version=1.5` — the old step would have failed); new `distro-smoke` job runs the script on the built x86_64 artefact in 5 containers with `timeout 300` each. AppStream: no warnings; one pedantic hint (uppercase cid) left as is. `xarast-cli` is not in the image → XARA-T-0049.
