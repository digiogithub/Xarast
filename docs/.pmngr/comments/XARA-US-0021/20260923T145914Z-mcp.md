---
type: comment
item: XARA-US-0021
author: mcp
created: 2026-09-23T14:59:14Z
kind: comment
---

Round 1 summary (branch worktree-agent-a53a3f08b4d5f06bc, rebased on 86c1d73). F1.1–F1.8 done (XARA-T-0063..0070) in 0529f6d, backend correction in c89d655; fuzz target XARA-T-0091 (7238459).

- `PackageWriter` (normative order, mimetype first/STORED/no extra field, deterministic mode, raw copies) and `XarastReader` (64-byte sniff, end-record pre-check, name validation that rejects, duplicate detection, zip-bomb/total/entry limits, capped reads, never parses document.svg).
- Tests: `crates/xarast-format/tests/container.rs` (magic bytes, round trip, byte-identical deterministic save, byte-identical re-save from the package, zip-slip, duplicates, EFS, bombs, ZIP64 66k entries, golden digest).
- Fuzz: 30+ min, 10.2 M execs, no findings.
- Leftover: XARA-T-0090 (workspace DEFLATE backend: miniz_oxide now, zlib-rs is ~3× faster but must be switched for every crate at once).
