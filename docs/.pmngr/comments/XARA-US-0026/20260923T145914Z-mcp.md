---
type: comment
item: XARA-US-0026
author: mcp
created: 2026-09-23T14:59:14Z
kind: comment
---

Round 1 summary. F6.1+F6.2 and F6.3 done (XARA-T-0084, XARA-T-0085) in 6e85a50.

- `write_atomic[_with]`: O_EXCL temporary in the same directory, fsync, optional `.bak`, rename, directory fsync; follows symlinks, keeps permissions; fault-injected at 7 points + 2 writer failures, original always byte-identical and no temporary left. Document-level `save_atomic(doc)` becomes `write_atomic(path, |f| writer.finish_with_source(f, src))` once W3 exists.
- `DocumentLock`: §10.4 text, O_EXCL + `File::try_lock` (no fs4), stale = same host + boot id + dead pid + free flock, `steal_if_stale`, `force`, `Unavailable` for read-only directories (open unlocked); release checks inode so a forced lock is never deleted by the loser.
- Leftovers: XARA-T-0086 (F6.4 lock UX + SIGINT/SIGTERM cleanup, app), XARA-T-0087 (F6.5–F6.7 autosave/journal/recovery), XARA-T-0088 (F6.8 repair, F6.9 partial XML recovery).
