---
type: comment
item: XARA-US-0027
author: mcp
created: 2026-09-23T19:55:15Z
kind: comment
---

Progress from XARA-US-0084 (commit 434916d):
- F7.1: `packaging/linux/xarast.xml` already had the three-level magic (0/30/38); priority raised 70 → 80 as the plan says. Verified with `update-mime-database` into a scratch `XDG_DATA_HOME` + `gio info`: a saved package with **no extension** is detected as `application/vnd.xarast+zip`.
- F7.2: `xarast.desktop` already lists the MIME types (desktop-file-validate clean). The New Document action is still missing.
- F7.3: `xarast-thumbnailer` (xarast-cli bin): extracts `thumbnail.png` only (checked against the 512 px cap, box-filtered to `-s SIZE`), never parses or renders; `packaging/linux/xarast.thumbnailer` registers it. Unit-tested.
- F7.4: `build-appimage.sh` builds and installs both into the AppDir (`usr/bin`, `usr/share/thumbnailers`) and passes the thumbnailer to linuxdeploy. Not yet run in a built image; host integration still needs appimaged or equivalent.
- Schemas (F7.6/F7.7) and the save-boundary normalisation are untouched.
