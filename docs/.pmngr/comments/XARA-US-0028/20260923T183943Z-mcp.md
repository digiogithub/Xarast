---
type: comment
item: XARA-US-0028
author: mcp
created: 2026-09-23T18:39:43Z
kind: comment
---

Ready for review (branch worktree-agent-ad00a7bebba8859c3: d458c54, 1cef5aa, d969aa3, 27c426c).

With bitmaps decoded, the corpus round trip .xar → .xarast → reload is exact again: **model 59/59** (normal form now includes each bitmap's palette hash), **bytes 59/59** (first re-save byte-identical), **render 59/59 pixel-identical** (CPU, 480×360). `KNOWN_RENDER_GAPS` and its mechanism are deleted from `crates/xarast-app/tests/xarast_roundtrip.rs`.

- XARA-T-0154 (done): the JPEG8BPP snap palette is a `resources/blobs/` blob named by `xarast:palette`; the JPEG stays browser-readable.
- XARA-T-0157 (done): same root cause — scope3's wood-tile JPEGs are tag 71.
- XARA-T-0155 (done): the contone fill already came back exact; a browser now draws it through a duotone filter.
- Browser rendering: a bitmap transparency is now also a luminance `<mask>` (scope3/JagSS100 shadows were black boxes in resvg). resvg conformance vs Xarast's own render: mean SSIM 0.971, 34/59 ≥ 0.99; JagSS100 0.941 → 0.969, scope3 0.900 → 0.926.
- New finding, filed as XARA-T-0171: the CPU backend draws bitmap fills/transparencies vertically flipped relative to the documented mapping (resvg is upright). The round trip cannot see it.

Gates: fmt, clippy -D warnings, `cargo test --workspace` with the corpus 1283 passed; ProbeX16 save 0.79–0.82 s.
