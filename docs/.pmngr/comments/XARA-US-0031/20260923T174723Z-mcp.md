---
type: comment
item: XARA-US-0031
author: mcp
created: 2026-09-23T17:47:23Z
kind: comment
---

Geometry half done (branch `worktree-agent-a369bd3e60053d6ee`, based on 003fd5f, commits 7151c9e..2b066b1, not pushed).

- **XARA-T-0148 (done):** `hit_fill`/`hit_stroke` (+ `_transformed`) and `HitShape`/`HitTolerance` in `xarast_geom::hit`. Covers every fill rule, caps, joins, dashes (gaps miss), hairlines at a minimum pick width, and transforms. Also fixed `stroke_to_path` giving both ends the union of mismatched caps, and `fill_contains` ignoring the implicit close of open subpaths.
- **XARA-T-0149 (done):** `HitIndex<K>`, a hashed grid. Pick candidates come out nearest the viewer first; marquee supports touch and enclose; edits cost O(cells).
- **XARA-T-0150 (done):** grid beats a static BVH at 100k. The BVH queries faster, but both are far inside budget, and the BVH needs an 8–23 ms rebuild per insert or delete. Budgets: precise pick 5.5–13.8 µs; whole-page marquee ≤ 1.38 ms. An adversarial stack of 100k hollow outlines takes 18–21 ms: every candidate needs a precise rejection, which no index can avoid.
- **XARA-T-0151 (done):** `fuzz_hit_test`, now in the nightly workflow. Its one finding is fixed and has a regression test; a 10-minute run afterwards was clean.

Remaining, and the reason this stays in_progress: **XARA-T-0152** (app side, todo). The integration contract is in `docs/memory/geometry.md` under "Integration contract for `xarast-app`". Snapping to paths (W8) is filed as XARA-T-0153 (backlog).
