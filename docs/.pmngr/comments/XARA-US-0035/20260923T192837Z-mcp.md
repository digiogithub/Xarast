---
type: comment
item: XARA-US-0035
author: mcp
created: 2026-09-23T19:28:37Z
kind: comment
---

Ready for review (branch worktree-agent-a67f966fcb3c4b9c3: b778a4f, bb54c2a, 5990704, f841c20, 2afad12, 44d84e8, 7ce1f47).

- `xarast-app/src/structure.rs`: Group/Ungroup (localise via `resolve_inherited` before/after every move, `factor_out` on group), z-order ×6 (front/back/forward/backward, layer up/down to the next editable layer), align/distribute (9 anchors, distribute min/centre/max/gaps, to selection/page/first selected), Duplicate (right above, 10 pt/−10 pt offset), Cut. No-op ops record no undo step.
- Clipboard: Copy builds a self-contained fragment document (kept in `AppState`) and puts its `.xarast` SVG profile on the system clipboard as text; Paste reads the clipboard via `PlatformRequest::ReadClipboard` → `Intent::PasteText`: our own fragment when the text is ours (or no clipboard), otherwise the text read as SVG. Paste centres in the view; Paste in place keeps coordinates. Colours resolved / bitmaps imported across documents.
- UI: Edit (Cut/Copy/Paste/Paste in place/Duplicate), new Arrange menu, Alignment panel (Ctrl+Shift+L). Keys per research/04 §4.2–4.3; no clashes (test).
- Fixed on the way: `Tx::move_node` charged the whole subtree to the undo budget, so grouping a big drawing evicted its own undo step (f841c20).
- Evidence: tests/structure.rs (10, every op undone to the canonical digest, pick index checked equal to a fresh one), tests/structure_corpus.rs (group∘ungroup pixel-identical + z-order + undo on 41 corpus files — acceptance criterion 10), bench group 5000 + undo 10 ms, align+distribute 5000 + undo 1.7 ms (budgets 50 ms). Live COSMIC `--probe arrange` p50 2.32/p99 4.32 ms with the Alignment panel open; system clipboard round trip in an isolated GNOME 46 session (`read=Ok(12046) ours=true`).
- Leftovers filed as tasks: image/svg+xml + PNG clipboard flavours and image paste; clone, paste attributes, duplicate-offset preference.
