---
type: comment
item: XARA-US-0036
author: mcp
created: 2026-09-23T19:28:46Z
kind: comment
---

Ready for review (branch worktree-agent-a67f966fcb3c4b9c3: bb54c2a, 5990704, 2a96dff, b7cc164, 44d84e8, 7ce1f47).

- `xarast-app/src/snap.rs`: `SnapSource` trait, `SnapResolver` (per-axis nearest within a radius in device px, tie priority guide > object > grid, point candidates vs axis pairs), `GridSource` (spread grid, minor spacing, integer-exact), `GuideSource` (visible guide layer), `ObjectSource` (through the pick index: box corners/edge middles/centres, then outline via XARA-T-0153).
- Applied through `ToolCtx::snap_point` / `snap_move`: move (nine anchors, not under Constrain), scale blob, rotation centre, rectangle/ellipse creation. Snap marker overlay (`HandleShape::Snap`).
- NumPad . / 2 / * toggle grid/guides/objects and work mid-drag (re-evaluate at once); `#` show grid; NumPad 1 show guides.
- Guides and grid are undoable document edits (`GuideCommand`); ruler guide add/move/remove and grid settings from the UI now reach the document; they survive `.xarast` save/reopen.
- Evidence: tests/snapping.rs (6: exact grid landing, mid-drag toggle on/off/one undo step, guide radius 5 px snaps / 12 px not at zoom 0.5/1/4, guides+grid round trip through .xarast + undo to digest, show/hide, object snap corner-then-outline), snap.rs unit (4). Live COSMIC `--probe snap` (grid+guide+object snapping on GardenPlan) p50 1.99 / p99 4.22 ms; screenshot shows grid, guide and marker.
- Leftovers filed: snapping for the shape editor/pen nodes and guide drags; guide properties dialog / Delete all guides menu item.
