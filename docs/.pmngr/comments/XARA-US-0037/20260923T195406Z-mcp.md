---
type: comment
item: XARA-US-0037
author: mcp
created: 2026-09-23T19:54:06Z
kind: comment
---

Model half done on branch `worktree-agent-abb102d2f54edacb4` (rebased on e07fe88): 4580d9b (research §5.10.1), d0b8d80 (conversions + table editing), 48807d8 (doc commands, resolver, digest), cab0944 (bench), 18281c6/9707db3 (memory), 096d200.

- Conversions now follow the original's shipped (NO_XARACMS) branch: CMYK black generation past 50 % (still exactly invertible), grey 0.305/0.586/0.109, per-model tints, signed HSV shades, the FIXED24 byte packing (`to_rgba8_packed`). Round-trip error ≤ 0.0001/255 everywhere.
- Corpus oracle: 5 760/5 760 palette entries resolve to their file's cached RGB exactly (round-to-nearest would miss 545). The `.xar` reader now keeps shade signs.
- `ColourTable`: redefine/rename/reparent/remove(OnDelete)/refresh_from/repair_cycles, cached resolve_order, monotonic PaletteEpoch; `table::cycles` over 1 000 random graphs clean. redefine 0.94 µs vs 20 µs budget; convert ≤ 9 ns vs 20 ns.
- Palette commands with digest-exact undo (palette now in the canonical digest); Detach delete leaves zero dangling refs.
- Done tasks: T-0195..T-0199. Filed: T-0203 (walker wiring), T-0204 (no colour), T-0205 (.xarast palette round trip).
- Gates: fmt, clippy -D warnings, 1 449 workspace tests with the corpus (one timing test in freehand flaked once under load avg 60, passes on rerun).
