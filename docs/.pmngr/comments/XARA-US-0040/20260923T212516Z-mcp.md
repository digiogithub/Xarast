---
type: comment
item: XARA-US-0040
author: mcp
created: 2026-09-23T21:25:16Z
kind: comment
---

Partly done on branch `worktree-agent-af22d8fa4761300d3`. Nothing is pushed or merged.

**Commits**
- 1324f26 render: evict ramps least recently used and draft a dragged fill's ramp
- 9697a10 docs: record the ramp contract and ramp eviction in the render note

**Criteria**
- **T8.5.1 was already met in phase 4.** `build_ramp` and `build_transparency_ramp` are the only ramp builders, and both backends use them through `RampCache`. The only bias/gain implementation is `BiasGain::map`. The identity short-circuit is now pinned by a test that checks all 2048 entries are exactly linear.
- **T8.5.2 is done.** `RampCache` now evicts the least recently used tables: `begin_frame`, then `evict(budget)`. Tables used by the current frame are never evicted, and freed slots are reused. `Resolver::trim_ramps` also clears the transparency tables stored at the same ids. The walker runs this every rebuild with a 16 MiB budget.
- **T8.5.3 is done.** The session already had Draft = 256 and Final = 2048. Now any node being previewed by a gesture always builds a 256-entry ramp, so a new table on every drag frame stays cheap. Once the drag is released, the next frame rebuilds at the session's quality. A test in `fill_tool.rs` checks this: at rest the cache holds 2048×4 bytes, and mid-drag it grows by exactly 256×4.
- **T8.5.4 is not done**, filed as XARA-T-0221. The session still invalidates the whole viewport after every mutation, and per-command dirty regions would need a change to that shared plumbing.
- **T8.5.5 is not done**, filed as XARA-T-0222 (the golden-image matrix).

**Evidence**
- New unit tests in `ramp.rs`: LRU order and slot reuse, a cache with no frame started never evicts, identity is exactly linear.
- New unit test in `cpu.rs`: trimming clears the matching transparency tables.
- Gates all pass (1568 tests passed).

Moving this to in_review with two of the five tasks still open. Please decide whether it can close with XARA-T-0221 and XARA-T-0222 carried as follow-ups.
