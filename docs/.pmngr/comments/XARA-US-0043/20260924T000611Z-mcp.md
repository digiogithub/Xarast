---
type: comment
item: XARA-US-0043
author: mcp
created: 2026-09-24T00:06:11Z
kind: comment
---

## Report (branch `worktree-agent-a8332b0cf0245a057`, 6 commits on e5d71fc)

- **T8.8.1** `crates/xarast-cli/tests/fills.rs`: for all 59 files, the fill, transparency, line-colour and line-transparency records by shape, plus the effect and mapping records, match the model's attributes exactly: 355 831 fills. Records inside `TAG_CURRENTATTRIBUTES` are excluded, and so are records inside `TAG_TEXT_LINE`, because the text importer folds them into runs. **Gap fixed:** definitions inside a dropped atomic subtree were lost. Groucho2 defines a bitmap inside a shadow, and 3 contone fills imported as no colour. They are now kept (`RecordTree::rescued`; snapshots updated). Known gap: a 3-point linear fill's third point is still dropped. All 27 in the corpus are within 1.2° of perpendicular, so the render is unchanged. Filed as XARA-T-0257.
- **T8.8.2 / criterion 9** `crates/xarast-format/tests/fill_round_trip.rs` covers every colour and transparency shape with a 7-stop ramp, 3 profiles, sin mapping, the 3 effects and the 5 mappings. On save → load, every fill is equal to the model's. On save → load → save, the package is byte-identical. **Bugs fixed:** profiles are now written in their shortest exact form. The SVG reader now rounds decimals correctly: `.35` used to read as 0.35000000000000003.
- **T8.8.3** The baked ramp was off by up to **64/255** on a steep profile. The baker is rewritten: stops on the 1/10 000 offset grid, forced breaks at key kinks, quarter-point probes. Measured worst error: **1.9/255** at 4 097 samples over 32 ramps. The reader still discards baked stops. Criterion 9 proves this.
- **T8.8.4** `xarast-cli inspect --fills [--summary]` handles .xar and .xarast. Its histogram equals the tag census, which is the `xar-dump --tags` families without current attributes.
- **XARA-T-0102, partly (XARA-T-0258):** conical, diamond and 3/4-colour fills, plus conical and diamond transparencies, are baked into SVG geometry (`svg/bake.rs`). Corpus SVG export vs PNG: Fill Types simple 19.7 → 1.9, WATCH2 16.5 → 0.7, WATCH 5.1 → 2.4. Nothing regressed, and the 3 SVG limits were dropped from `xtask/export-limits-corpus.txt`. Corpus SVG is 3 % larger.

Gates: fmt ok; clippy -D warnings ok; `cargo test --workspace` with corpus: 1 690 passed, 0 failed, 1 ignored; deny ok; build --locked ok; export-check 59/59.

Not done: the merge of the updated working branch (2bbc6f2, US-0048) was denied by the permission system. It must be done before this lands. Not covered here: XARA-T-0222 (render goldens), mesh transparencies and mesh tiling in the renderer (XARA-T-0256), and the rest of T-0102 (BakeProvider, filters, rasters).
