---
type: comment
item: XARA-US-0045
author: mcp
created: 2026-09-23T18:48:52Z
kind: comment
---

Text integration round (branch `worktree-agent-a8925f0d919c9d2c8`):

- **Model (read side):** `xarast_doc::text_model` — `StoryText::collect` (logical text, attribute runs, lines, kerns, item index), `TextPos` ↔ byte offset, `TextCursor`, `StoryFlow` (19d49d9).
- **Importer:** a text record's attributes now scope to its own characters (they used to leak to the next string); surrogate pairs across two `TAG_TEXT_CHAR` join (42a7e28); stories relying on the original's 16 pt default get it explicitly (9acfb5b).
- **Bridge + rendering:** attribute bridge to `StyleRange`/`ParagraphStyle` and the walker's text path (53899b6); tracking/manual kerns measured in the width of 'M' as the original does (b13af5a).
- `text_pending` 0 on the corpus; all 14 TextDesigns render and overlay their embedded reference bitmaps closely where metric-compatible fonts exist.
- Leftovers: XARA-T-0175 (edit commands, invariants, format_story), XARA-T-0174 (faux bold, underline metrics, bounds caches), XARA-T-0172 (`.xarast` text writer).
