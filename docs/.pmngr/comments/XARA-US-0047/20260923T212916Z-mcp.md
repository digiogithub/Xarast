---
type: comment
item: XARA-US-0047
author: mcp
created: 2026-09-23T21:29:16Z
kind: comment
---

Ready for review on branch `worktree-agent-a618651384072148e` (based on 353e880). Not merged or pushed.

**Commits**
- eab83e1 core: expose each line's base direction and word segments (`LaidLine::base_rtl`, `xarast_text::word_segments`)
- 2299756 ui: draw text carets and selection highlights over the canvas (`OverlayItem::Caret`/`Highlight`, blink)
- 4829100 core: pick text stories by the box of their laid-out lines (the selector could not click text before this)
- 8a44634 core: the text tool: caret, selection and bidi navigation (`text_edit.rs` CaretMap, `text_tool.rs` TextTool, `Intent::TextNav`, shell key routing, F8)
- 7d626b8 core: keep logical caret moves local and benchmark caret movement
- 4afc4ea docs: memory notes (text.md "The text tool", tools.md decisions 45–48 + invariants 10–11, ui.md decision 40)
- e90efbd ui: the palette publishes the text tool

**Criteria**
- T9.4.1 state machine: done. Click text → caret; drag → select; double click → word; triple click → line; Shift+click extends; click empty canvas → pending point story; drag → pending column; Esc leaves the text; Ctrl+A selects the story; selector double click on text opens the tool. Nothing touches the document (digest and undo history unchanged, tested).
- T9.4.2 caret: done. Blinks (0.53 s, stops after 10 s idle); split caret at direction boundaries (primary = the stop the caret is on, secondary drawn half height).
- T9.4.3 selection: done. Per-line spans in visual order, plus a block for a selected paragraph break; turned with rotated stories.
- T9.4.4 keyboard: done. Visual arrows by default (logical is a preference), Ctrl word, Home/End, Ctrl+Home/End, Up/Down keeping the goal x, PgUp/PgDn, Shift extends. The shell routes these keys to the caret instead of panning, and plain character keys don't fire shortcuts while a caret is up.

**Evidence**
- tests/text_caret.rs (15), including the test plan's bidi caret walk over every line of hebrew.xar (corpus), all asserted on byte offsets
- tests/text_tool.rs (10, through Session intents)
- shell `a_text_caret_takes_the_navigation_and_character_keys`, ui `the_caret_blinks_from_on_then_stops_blinking`, xarast-text base-direction tests
- Bench `text_caret` (10k chars, budget 1 ms): visual 0.72 µs, logical 2.1 µs, word 1.1 µs, line 1.2 µs, hit 0.55 µs
- Gates: fmt, clippy -D warnings, workspace tests with corpus (1567 passed, 0 failed, 1 ignored), cargo deny, build --locked all pass (exit 0)

**Follow-ups:** XARA-T-0223 (typing and deletion; creates the pending story), XARA-T-0224 (IME and clipboard), XARA-T-0225 (infobar and ruler), XARA-T-0226 (manual live-window check; no windows were launched).
