---
type: comment
item: XARA-US-0048
author: mcp
created: 2026-09-23T23:52:05Z
kind: comment
---

## Report (branch worktree-agent-a8503fe8af55ac631, 7d78aa9..9be6e4d)

**Open question 4 is decided (2026-09-24): we do not need our own layout pass.** The story is laid out straight, and then each cluster is placed on the path by arc length (spike A, `xarast-text/src/path.rs`: `TextPath`, `PathFit`). The original fits text to a path the same way.

**T9.5.3 measurement** (`xarast-app/src/text_path_fidelity.rs`, pinned fonts). For each glyph it measures how far the glyph's origin moves, as a fraction of its advance. The reference is the original's placement rule, evaluated independently. Pass means p95 ≤ 0.25 and max ≤ 0.5.
| Fixture | A p95/max | B p95/max |
|---|---|---|
| tight open arc (r=2 em) | 0.0021/0.0021 | 3.14/3.14 |
| closed circle, full justification | 0.0013/0.0014 | 5.29/5.81 |
| Designs/TextCurve.xar (1388 glyphs) | 0.0007/0.0015 | 1.30/3.32 |
| AngledText.xar, Rotated.xar | no story on a path: 0/0 | 0/0 |
Spike A passes on all five fixtures. Spike B is kept only as the comparator in this test.

**What changed:**
- **Model:** `TextLayout::OnPath::chars: CharsTransform` (reflected, rotation, shear).
- **Importer:** now maps START/END × LEFT/RIGHT correctly. It had read "reversed = odd tag"; the corpus did not hit the cases where that was wrong.
- **`.xarast`:** writes `xarast:path-params` and reads it back.
- **Walker:** fits the text to the story's first Path child and draws that path under the text.
- **Picking and framing:** use the fitted boxes.
- **Convert to shapes:** follows the path (XARA-T-0246).
- **Corpus:** `text_on_path_pending` is 0 for every file.

Facts and `file:line` references are in `docs/memory/text.md`, "Text on a path". The architecture §7 table and the phase doc are updated.

**Gates:** fmt ok, clippy ok, tests 1693 passed / 0 failed / 1 ignored (with corpus), deny ok, build --locked ok.

**Still open (filed as tasks):**
- XARA-T-0250: text tool carets, hit tests and selection along the path.
- XARA-T-0251 (T9.5.5): editing: fit to path, remove, reverse, indents, word wrap on a path.
- XARA-T-0252 (T9.5.6): base SVG `<textPath>`.

**Known limitation, matching the original:** letters crowd together on the inside of tight curves.
