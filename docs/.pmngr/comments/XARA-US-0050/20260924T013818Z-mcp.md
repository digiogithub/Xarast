---
type: comment
item: XARA-US-0050
author: mcp
created: 2026-09-24T01:38:18Z
kind: comment
---

## Report (branch `worktree-agent-a2fc6bde7ef5565ef`, commits ed04d29, c3a707f)

| Task | Phase criterion | State |
|---|---|---|
| T9.7.1 tag inventory | 3 | **met**: `xarast-cli/tests/text_designs.rs` runs `xar-dump --tags` on all 14; no text tag in 2100-2117/2200-2204/2900-2920/4200-4207 lacks a decoder. The per-file inventory is in `docs/memory/text.md`, and the phase-09 table notes where the files differ from its "exercises" column (XARA-T-0260) |
| T9.7.2 golden renders | 2 | **met as a regression gate**: `xarast-cli render --zoom 200`, pinned fonts, 14/14 rendered, nothing pending, and each render matches its committed SHA-256 exactly (gate A). No missing-glyph boxes. Only digests are committed: the corpus renders contain Xara's reference bitmaps |
| T9.7.3 round trip | 12 | **met** earlier (XARA-T-0172): `xarast-app/tests/xarast_roundtrip.rs` gives zero differing pixels on all 59 files, these 14 included |
| T9.7.4 WOFF2 + fsType | 13 | **open**: fonts are not embedded in `.xarast` yet (XARA-T-0218, backlog) |
| T9.7.5 multi-script | 15 | **open**: no Devanagari or Thai face in the pinned set (filed XARA-T-0261) |

Also filed: XARA-T-0262. With the pinned set every family falls back to Noto Sans, so the goldens do not overlay the original's reference bitmaps. Checking against the original (gate C) needs metric-compatible pinned faces.

Criteria 4-8 belong to the W9.3 stories and are not part of this story.

Gates: fmt, clippy -D warnings, workspace tests with the corpus (1718 passed, 0 failed, 1 ignored), cargo deny and build --locked all passed. No production code changed.
