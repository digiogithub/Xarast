---
type: comment
item: XARA-US-0051
author: mcp
created: 2026-09-23T17:16:12Z
kind: comment
---

Round 1 (xarast-image) ready for review. Branch `worktree-agent-a4a1a11e92b2cd07e`, commits c5605ee, 6f16ebc, 7d35eff, 2c9b3bf.

- Decoding façade in the leaf crate `xarast-image` (`image` 0.25.10, default features off, png/jpeg(zune-jpeg 0.5)/webp/gif/tiff/bmp/pnm; no kamadak-exif, no jpeg-decoder). All licences MIT/Apache/Zlib/BSD; `cargo deny` clean with no new exception.
- Guards from the header before allocation: dimension, pixels, decoded bytes (overridable `TooLarge`), ratio 20 000:1, JPEG scans 128, allocation ceiling 1.25× native + 1 MiB, cooperative deadline + hard `decode_on_worker`. 11 synthetic bomb fixtures refused in µs; suite peak RSS +32 MiB.
- EXIF: all 8 orientations normalise to identical pixels and BLAKE3 hash.
- Corpus: every bitmap record in the 59 files decodes (58 GIF previews, 4 JPEG, 32 PNG, 8 JPEG8BPP; 0 failures). New facts in research/01 §4.5 (tag 65 is a headerless DIB; tag 69/71 semantics).
- Fuzz `fuzz_image_decode`: one finding (tag-71 palette snap on a translucent image) fixed with a regression test; then 601 s / 4.69 M execs clean. Added to the nightly workflow.
- Benches (load 25–28): probe JPEG 59 ns / PNG 390 ns; 24 Mpx JPEG 184 ms (≤ 400); 24 Mpx PNG 227 ms (≤ 600); 4K PNG 50 ms; BLAKE3 100 MB 29 ms.
- Remaining for the story: T10.1.2–T10.1.4 (xarast-doc) and the walker wiring XARA-T-0129 (contract in docs/memory/image.md).
