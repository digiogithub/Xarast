---
type: comment
item: XARA-US-0055
author: mcp
created: 2026-09-24T10:08:34Z
kind: comment
---

Overlap with XARA-T-0272 (T10.3.8), which landed first: the **drop-file-on-canvas** and **paste-from-clipboard** halves of T10.7.4 are done there (`Intent::ImportImage` / `Intent::PasteImage`, `xarast-app/src/place.rs`, shell drop and paste plumbing in `viewer.rs`; `tools.md` decisions 68–69, `ui.md` panels decision 28). Placement decodes once under the default limits, stores PNG/JPEG/GIF bytes as they are (others as lossless PNG), sizes at natural size from DPI, one undo step.

Still for this story: the XDG-portal **file dialog** import (T10.7.4's third path), progress/cancel for slow imports (T10.7.5), the gallery itself (T10.7.1–3, T10.7.6), dragging a gallery bitmap to the canvas or onto an object (the original's bitmap drag → bitmap fill), and pasting a copied image *file* (`text/uri-list` from a file manager).
