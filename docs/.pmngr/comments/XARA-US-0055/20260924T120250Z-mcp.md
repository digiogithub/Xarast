---
type: comment
item: XARA-US-0055
author: mcp
created: 2026-09-24T12:02:50Z
kind: comment
---

## Report (branch `worktree-agent-ae701e0172dda849a`, 5 commits on 2ea1e92)

- `5dd08c4 core:` `xarast_doc::bitmap_usage` (live vs history-retained refs), `remove_unused_bitmap`, `DocumentResources::bitmap_key`.
- `445be0c core:` `xarast-app::bitmap_gallery` (entries with uses/memory/colour space, off-thread thumbnails keyed by content hash, Place, Delete unused-only, drag → bitmap fill on a non-bitmap object at natural size centred on its bounds, or placement on empty canvas / a bitmap object; facts `Kernel/sgbitmap.cpp:428-583`, `Kernel/fillattr.cpp:14259-14330`); `import` worker (files > 1 MiB read in chunks + decoded off-thread, progress, cancel, drop point kept in doc space); file-list paste (`place::image_paths_in_text`: uri-list, gnome-copied-files, paths); `AppCommand::Import` (Ctrl+Shift+I) → `PlatformRequest::ShowImportDialog`.
- `e5acc46 ui:` bitmap gallery panel (thumbnails, details, filter, sort, Place/Delete, draggable rows), File › Import…, status-bar import progress + Cancel.
- `2d211fe ui:` shell: parented multi-file Import portal chooser, file-list text beats a clipboard picture, `poll_imports`/thumbnail redraws in housekeeping, gallery drag mapped to canvas.
- `2cd90ae docs:` image.md, ui.md (gallery section, decisions 29, 42), tools.md (decisions 70–73).

Tests: every gallery drop/place/import is one undo step with exact undo (resource insertion outside undo, XARA-T-0283); gallery drops as a fill and as a placement repaint only the target (`xarast-shell/tests/bitmap_damage.rs`, +2); usage through group/ungroup/delete/undo and Delete refusal (acceptance 15 partly: `collect_unused` after prune was already covered).

Gates: fmt ok; clippy -D warnings ok; `cargo test --workspace` with corpus 1916 passed / 0 failed / 1 ignored; cargo deny ok; build --locked ok; release `xarast` links only libc/libm/libgcc_s.

Colour-space slot: T10.8.1/.3/.4 were already done (image.md round 1); the gallery reports the space. Gaps filed: XARA-T-0289 (thumbnail disk cache + budget), XARA-T-0290 (Replace/Save a copy, T10.7.6), XARA-T-0291 (T10.8.2 ICC as resources; converted PNGs lose iCCP), XARA-T-0292 (F11, Ctrl-drop background, rename, live desktop check).
