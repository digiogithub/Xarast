---
type: comment
item: XARA-US-0058
author: mcp
created: 2026-09-23T22:16:54Z
kind: comment
---

## Report (worktree branch `worktree-agent-a631d97339989c4a9`, rebased on the working branch after XARA-T-0231)

**Built**
- T11.3.1: `SvgDialect { Native, Interchange }` on `xarast_format::svg::SvgOptions`, plus `area`, `background`, `minify` and a `BitmapLinker`. There is still one mapper. Interchange drops foreign baggage in the emitter (the count goes into `Stats::foreign_omitted`), then projects the finished text onto its base layer (`svg/interchange.rs`): no `xarast:` attributes or elements (an element goes with its subtree), no `xmlns:xarast`, `xlink:href` only on images. Native output is unchanged.
- T11.3.2/T11.3.3: `SvgExporter` in `xarast-io` (`FormatId::Svg`, `SvgOptions { resources: Inline|Sidecar, minify, pretty }`). `ExportSource` gained two methods with defaults, `document()` and `svg_text_placer()`. Bitmaps: PNG/JPEG/GIF originals pass through unchanged. Anything else is decoded by the walker's rules and written as PNG. `Inline` uses data URIs; `Sidecar` writes `<stem>_files/image-N.ext` (folder name sanitised). New compromises: `Simplified { what, count }`, `UnknownDataDropped`, `FontNotEmbedded`.
- CLI: `xarast-cli export … -o x.svg` with `--resources`, `--minify`, `--pretty`. There is now `cargo xtask svg-check [--interchange]`, which parses with usvg and renders with resvg.

**Validation**
- Corpus: 59/59 exported with no errors, and two runs are byte-identical. svg-check passes 59/59 inline and 59/59 sidecar+minify.
- resvg vs our PNG export (72 dpi, paper background): median mean |Δ| 1.30/255, and 52/59 files are under 4/255. Worst: Fill Types simple 20.0 and WATCH2 17.0 (fills the profile does not bake yet, XARA-T-0102), TextJust 14.9 (glyph rendering), leafgirl 12.7 (resvg draws seams between pattern tiles).
- Gates: fmt, clippy -D warnings, workspace tests with the corpus (1626 passed, 0 failed), cargo deny and build --locked all pass.

**Not built (tasks filed):** XARA-T-0233 fonts (WOFF2 or outlines), XARA-T-0234 precision, XARA-T-0235 Reference resources, physical size and full minify, XARA-T-0236 CI wiring of the resvg check and the comparison.
