---
type: comment
item: XARA-US-0060
author: mcp
created: 2026-09-23T23:06:08Z
kind: comment
---

## Report (branch `worktree-agent-ab717af6f28268d00`, commits cbfcc48..4440d05 on top of 5985c3e)

**Done**
- T11.5.1 markers: PNG `sRGB`/`iCCP`, JPEG EXIF ColorSpace, PDF DeviceRGB, SVG CSS sRGB, each asserted by a test. WebP stays without `ICCP` on purpose (a WebP with no ICCP chunk is sRGB; we have no sRGB profile of our own yet, see XARA-T-0249).
- T11.5.2: SVG keeps an image's ICC profile, either passed through byte for byte or, when it re-encodes the bitmap, in an `iCCP` chunk (`png::with_icc_profile`). Raster and PDF output report `Compromise::ProfileDropped` because the renderer does not colour-manage yet (phase 15).
- T11.5.3/T11.5.4: new `Compromise::ColourConverted { model: CMYK|spot, count }` in all five exporters (`xarast-io/src/fidelity.rs`). Nothing writes icc-color or DeviceCMYK, because no profile exists.
- T11.5.5: `xarast-cli fixtures` with a 20-patch colour sheet (RGB, CMYK, HSV, grey, palette CMYK, tint, local tint, spot), read back from all five formats. PNG, WebP, SVG and PDF are exact; JPEG is within ±6; Poppler's render is within ±1.
- T11.6.1: already covered by the CLI corpus test (59/59 in all five formats).
- T11.6.2/T11.6.3 (and XARA-T-0236): `cargo xtask export-check` renders SVG with resvg and PDF with Poppler and Ghostscript at the PNG's size, and compares the mean |Δ| against per-file limits (`xtask/export-limits-*.txt`). It also runs `qpdf --check`. On the corpus: SVG median 0.73/255, PDF median 1.98. The 20 pairs above 4 each have a limit and a reason in the limits file.
- T11.6.4: two processes produce identical bytes (test). The CI `export` job runs on ubuntu-24.04 and 22.04, and the `reproducible` job diffs their SHA-256 manifests.
- CI: the `check` job installs Poppler, so the PDF render tests now run. A nightly `export-corpus.yml` checks out the fork's corpus with a sparse checkout; it needs the `CORPUS_TOKEN` secret if the fork is private.

**Gates:** fmt 0, clippy 0, the workspace tests pass with the corpus (1646 passed, 0 failed, 1 ignored), deny 0, `build --locked` 0.

**Not met / follow-ups**
- Rendering the corpus against the original at 25/100/400 % is not done. The original cannot run here because it depends on the closed 2006 CDraw library. XARA-T-0248 compares our renders against the preview each `.xar` embeds instead.
- CMYK output with a profile (PDF DeviceCMYK, our own sRGB profile, icc-color) → XARA-T-0249.
- `qpdf` is not installed locally, so the first CI `export` job will be the first time `qpdf --check` runs on our PDFs.
- Finding: Poppler draws sub-pixel strokes as a whole pixel and mis-strokes the degenerate cap in `Broken Butt Cap` (12.6 where Ghostscript gives 0.97). That is why export-check uses both renderers.
