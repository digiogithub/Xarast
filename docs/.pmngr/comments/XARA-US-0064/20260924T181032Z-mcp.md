---
type: comment
item: XARA-US-0064
author: mcp
created: 2026-09-24T18:10:32Z
kind: comment
---

## Report (worktree branch `worktree-agent-ab2ee4449a814b200`, on top of 74aff7a)

Commits: 82a32a2 core (crash module, render-thread guard), 3571ca5 core (crash prompt, safe mode, `tests/crash_recovery.rs`), b3fd489 ui (device-loss rebuild, panic autosave, `--safe-mode`), 1e5d3a6 docs (app-core/ui/render notes).

- **F1** Local report `$XDG_STATE_HOME/xarast/crashes/crash-<ts>-<pid>.toml`: thread, location, message, backtrace, build, os, context (platform, renderer, adapter, safe mode) and the last 200 log lines. Paths are cut to their basename, and quoted text is scrubbed from run-time messages and from log lines. Nothing is uploaded.
- **F2** Device loss: the shell rebuilds the device from a CPU carry (the last canvas frame and view, a ledger of the egui textures). From the second loss on it uses CPU composition. A failed rebuild is retried with backoff and the loop never exits. Tested on wgpu's no-op backend (dev-dependency only). In a window, `XARAST_INJECT_DEVICE_LOSS=N` simulates a loss.
- **F3** A render-thread panic raises a fatal watch; a panic on the UI thread autosaves through the viewer's `Drop`. Both reuse `emergency_shutdown`, and the process exits 101. **Deviation:** the recovered copy is the autosave entry, not a `*.xarast.recovered` file (see app-core.md "Crashes").
- **F4/F6** Per-process sentinel with a crash history. Three crashes in 10 minutes force safe mode.
- **F5** `--safe-mode`: software adapter where there is one, CPU tier, prefs reset in memory. It can also be chosen from the crash prompt, which rebuilds the device live.
- **F7** The first prompt after a crash shows the report path, with Use safe mode, Copy report path and Continue, then leads to the recovery question.
- **F8** `XARAST_FORCE_PANIC=startup|ui|render`, compiled into release.

Gates, with the corpus: fmt, clippy, test (2064 passed, 0 failed, 1 ignored), doc and deny all exit 0. The release `xarast` binary links only libgcc_s, libm and libc (no wayland, GL or Vulkan). `XARAST_FORCE_PANIC=startup` on the release binary wrote a report and exited 101.

Gaps filed: T-0309 (build id), T-0310 (subsetter spurious report), T-0311 (real driver-reset/suspend soak), T-0312 (safe mode vs future persisted layout/theme/gallery), T-0313 (manual text). Not done: nextest is not used (`cargo test -p xarast-app crash_recovery` covers criteria 12 and 13). F7 has no separate dialog; it uses the generic modal prompt.
