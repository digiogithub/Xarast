---
type: comment
item: XARA-US-0082
author: mcp
created: 2026-09-23T14:14:32Z
kind: comment
---

## Summary (branch worktree-agent-abf929ee94a663741, based on b86bff4)

Commits: d77ff7f core (command table, app intents, recent store) · d78a841 ui (menu bar, About, empty state) · 2075afe ui (heading colour) · 0d9c1f6 ui (portal parent window, XARA-T-0048) · cdbb83f ui (shell wiring) · b26c5f1 docs.

- **Menu bar** drawn in-window by egui (top panel; no native/global menu). File: Open… Ctrl+O, Open Recent ▸ (list + Clear Recent Files), Close Ctrl+W, Quit Ctrl+Q. View: Zoom in (+), Zoom out (-), Fit page (0/Home), Fit drawing (D), 100 % (1). Help: About Xarast… (version, `MIT OR Apache-2.0`, not affiliated with Xara Group Ltd, third-party licences placeholder). Close/View greyed out with no document.
- **Commands** are `xarast_app::AppCommand` (label + key chords); menu and keyboard raise the same `Intent`s. `ShowOpenDialog`/`Quit` come back as `PlatformRequest`s the shell performs; the UI never calls the portal.
- **Open**: portal FileChooser, filters "Xara documents (*.xar)" + "All files", parented to the window (`wayland:<xdg-foreign handle>` / `x11:<xid>`). Chosen file replaces the document (a failed open keeps the current one; error in status bar + problem list).
- **Empty state**: "No document open", Open… button, Ctrl+O/drop hint, recent files.
- **Recent files**: `$XDG_STATE_HOME/xarast/recent` (fallback `~/.local/state/xarast/recent`), max 10, pruned, corruption-tolerant, atomic write.
- **Shortcuts** do not fire while a text field has the keyboard.

Tests: workspace 970 pass (was 932) with the corpus; clippy, fmt, `cargo deny check licenses` clean; release binary links no libwayland/GL/Vulkan.

Screenshots (scratchpad `/tmp/claude-1000/-www-Xarast-Xarast/603743a5-5559-460e-ae8b-4bf99eddcc71/scratchpad/`): `empty.png` (COSMIC, scale 1, no file, before the heading fix), `empty-recent.png` (COSMIC, recent list), `with-file2.png` (COSMIC, amurdove.xar), `iso/empty-125.png` and `iso/file-125.png` (isolated GNOME 46 at 1.25, private bus + private dconf profile file).

## Manual checklist (live dialog not driven: no input on the maintainer's desktop)
1. `xarast` with no file → menu bar + empty state.
2. Ctrl+O (and File › Open…, and the Open… button) → a chooser titled "Open", attached to the Xarast window, "Xara documents (*.xar)" selected, "All files" offered.
3. Pick a `.xar` → it replaces the document, title "name — Xarast", it heads File › Open Recent.
4. Escape → nothing changes. A non-.xar through "All files" → status bar "Could not open …", old document stays.
5. Ctrl+W → empty state; Ctrl+Q quits. Rename a layer and type "1", "d", Ctrl+W → nothing fires.
6. Help › About Xarast… → licence and non-affiliation text; Close.
