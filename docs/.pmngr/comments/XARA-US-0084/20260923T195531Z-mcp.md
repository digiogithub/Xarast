---
type: comment
item: XARA-US-0084
author: mcp
created: 2026-09-23T19:55:31Z
kind: comment
---

Ready for review — branch `worktree-agent-a51e2ae881ae106d1`, rebased on `claude/xara-xtreme-rust-port-ktggmu` at e07fe88. Commits: e49a5f6 (history state serial), f196095 (`SaveOptions::thumbnail`), b099fe4 (app core: save, dirty, locks, autosave, recovery, thumbnail), 2323b50 (ui: menu items, modal prompts), b9e7d48 (shell: save dialog, title, close, signals), 2895c19 (`prepare_save`/`prepare_resave`), 8f7487f (thumbnail beside the SVG serialisation), 9831b7a (exit 128+sig), 434916d (pkg: thumbnailer, MIME), 1575647 (docs), 7009fd7 (keypad shortcut fix).

**Criteria**
- Save (Ctrl+S) / Save As… (Ctrl+Shift+S) → parented portal chooser, filter "Xarast documents (*.xarast)", name prefilled; `.xar` (and untitled, read-only) asks for a name; `.xar` never written, a chosen name becomes `.xarast`. Off the UI thread (snapshot ~54 ms on ProbeX16 is the only UI cost); status "Saving…", "Saved name (size, ms)", "Could not save …"; a failure never touches the in-memory document or the target file. **Ctrl+Shift+S was Convert to shapes (phase 7) — moved to Ctrl+Shift+C** (tools.md decision 30).
- Dirty = `History::state_serial` ≠ the saved one: undo back to the save clears `•`, redo sets it, edits during a save keep it. Save/Discard/Cancel on Close, Quit, Open-over-dirty and the window's close button.
- Locks (T-0086), autosave + recovery (T-0087, no journal → T-0217), thumbnail on every save (T-0082): see the task comments.
- Budget: ProbeX16 save 1.09 s wall on tmpfs (format 0.86 s + restore 0.13 s) — ~9 % over, tracked in T-0215.

**Evidence**: `cargo test --workspace` with the corpus: 1448 pass, 0 fail; clippy `-D warnings`, fmt, `cargo check --locked`, `cargo deny` (signal-hook added) clean; release `xarast` links no libwayland/GL/Vulkan. Corpus: the snapshot save path writes byte-identical packages to a direct save for all 59 files (first save and raw-copy re-save), valid thumbnails for all 59. Live on COSMIC: `xarast --screenshot` of a saved `.xarast` (Groucho2) renders correctly; a real window holding a `.xarast` lock, `kill -TERM` → autosave+release in 214 ms, lock gone, exit 143.

**Manual checklist** (not driven: no input on the live desktop; T-0216)
1. Open a `.xar`, draw a rectangle → title `• name.xar — Xarast`.
2. Ctrl+S → "Save As" chooser attached to the window, only "Xarast documents (*.xarast)", name `name.xarast` in the file's folder → Save → status "Saved name.xarast (…)", marker gone, `.xar` untouched; file manager shows the thumbnail once the thumbnailer is installed.
3. Ctrl+Z → marker back; Ctrl+Shift+Z → gone. Ctrl+Shift+C converts a selected rectangle (not Ctrl+Shift+S).
4. Edit, Ctrl+W → "Unsaved changes": Cancel keeps it; Discard closes; Save saves then closes. Same with Ctrl+Q and the window's close button.
5. Open the same `.xarast` in a second `xarast` → "Document in use": read-only (title `(read-only)`, Save asks for a name), copy ("name (copy)"), force.
6. Edit, wait > 60 s idle, `kill -9` the process → next start offers "Recover unsaved work"; Recover opens it modified under its name; Save removes `~/.local/state/xarast/autosave/<id>`.
